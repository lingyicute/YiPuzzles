<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Square board card: 0dp/0dp + 1:1 ratio keeps the grey background exactly as big as the
         drawn board, in portrait and landscape alike. The packed chain reserves the space for
         the size label, so the card can never grow over it. -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/main_menu_card4"
        style="?attr/materialCardViewFilledStyle"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardBackgroundColor="?attr/colorSurfaceContainerLow"
        app:contentPadding="16dp"
        app:layout_constraintBottom_toTopOf="@id/main_menu_label4"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:shapeAppearance="@style/ShapeAppearance.Material3.Corner.ExtraLarge">

        <org.lyi.puzzles.views.BoardPreviewView
            android:id="@+id/main_menu_img4"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            app:boardSize="7" />

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/main_menu_label4"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="7x7"
        android:textAppearance="?attr/textAppearanceHeadlineSmall"
        android:textColor="?attr/colorOnSurface"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/main_menu_card4" />

</androidx.constraintlayout.widget.ConstraintLayout>
